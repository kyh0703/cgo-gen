version: 1

input:
  headers:
    - include/foo.hpp
  compile_commands: build/compile_commands.json

output:
  dir: out
  header: wrapper.h
  source: wrapper.cpp
  ir: wrapper.ir.yaml

naming:
  prefix: cgowrap
  style: snake_case
